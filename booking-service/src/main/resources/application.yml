spring:
  application:
    name: Seats
  profiles:
    active: ${PROFILE:local}
  datasource:
    url: jdbc:postgresql://dpg-d2fee36mcj7s73enqi40-a.oregon-postgres.render.com/spok
    username: spok_user
    password: VZih6lxGVwQsIfxU3O77PeqVXmuHDc1B
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  kafka:
    bootstrap-servers: localhost:9092
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8081

logging:
  level:
    root: ${LOG_LEVEL:info}
    org:
      springframework: INFO
  file:
    path: logs

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always

app:
  public-paths:
    - /api/**
  kafka-topic: seat_reservation
  seat-lock-ttl-seconds: 300

